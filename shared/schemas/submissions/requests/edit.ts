import { createSubmissionRequestSchema } from "#shared/schemas/submissions/requests/create";

// The edit schema is the same as the create schema, but we export the factory
export { createSubmissionRequestSchema };
